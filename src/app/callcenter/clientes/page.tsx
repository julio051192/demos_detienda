"use client"

import { Mail, Phone, Users } from "lucide-react"
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"
import { Badge } from "@/components/ui/badge"
import { useCallCenterStore } from "@/lib/callcenter-store"

export default function CallCenterClientsPage() { const { state, ready } = useCallCenterStore(); if (!ready) return <p>Cargando clientes...</p>; return <div className="space-y-6"><div className="border-b pb-4"><h1 className="text-2xl font-bold">Clientes multiservicios</h1><p className="text-sm text-muted-foreground">Ficha única para atender solicitudes de cualquier servicio.</p></div><div className="grid gap-4 md:grid-cols-2">{state.clients.map((client) => { const active = state.cases.filter((item) => item.clientId === client.id && item.status !== "resuelto").length; return <Card key={client.id}><CardHeader className="flex flex-row items-start justify-between"><CardTitle className="flex items-center gap-2 text-base"><Users className="size-4 text-cyan-700" />{client.name}</CardTitle><Badge variant={active ? "default" : "outline"}>{active} caso{active === 1 ? "" : "s"}</Badge></CardHeader><CardContent className="space-y-2 text-sm"><p className="flex items-center gap-2"><Phone className="size-4 text-muted-foreground" />{client.phone}</p><p className="flex items-center gap-2"><Mail className="size-4 text-muted-foreground" />{client.email}</p><p className="text-xs text-muted-foreground">{client.segment} · Último contacto: {client.lastContact}</p><p className="rounded-lg bg-muted/50 p-2 text-xs">{client.notes}</p></CardContent></Card> })}</div></div> }
